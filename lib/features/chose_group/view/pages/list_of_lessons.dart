import 'package:flutter/material.dart';

class ListOfLessons extends StatelessWidget {

  const ListOfLessons({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Scaffold(
        body: Text("Список предметов"),
      ),
    );
  }

}