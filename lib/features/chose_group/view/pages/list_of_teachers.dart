import 'package:flutter/material.dart';

class ListOfTeachers extends StatelessWidget {

  const ListOfTeachers({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Scaffold(
        body: Text("Список преподавателей"),
      ),
    );
  }

}