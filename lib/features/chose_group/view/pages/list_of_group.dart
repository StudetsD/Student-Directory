import 'package:flutter/material.dart';

class ListOfGroup extends StatelessWidget {

  const ListOfGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Scaffold(
        body: Text("Список группы"),
      ),
    );
  }

}